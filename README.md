راهنمای راه‌اندازی پروژه FacePong 🏓با سلام و احترام،این سند، راهنمای رسمی و کامل برای نصب، پیکربندی، و اجرای پروژه React با نام FacePong است. هدف از این مستندسازی، فراهم آوردن دستورالعمل‌های دقیق و استاندارد برای توسعه‌دهندگان جهت شروع کار با پروژه می‌باشد.۱. نیازمندی‌های اولیه سیستم (Prerequisites)پیش از آغاز فرآیند نصب، لطفاً از نصب بودن نرم‌افزارهای زیر روی سیستم عامل خود اطمینان حاصل نمایید:Node.js: به منظور اجرای محیط جاوا اسکریپت و ابزارهای بیلد. نسخه ۱۶ یا بالاتر قویاً توصیه می‌شود.لینک دانلود Node.js: https://nodejs.org/Git: جهت مدیریت نسخه‌ها و کلون کردن مخزن کد.لینک دانلود Git: https://git-scm.com/مدیر بسته (Package Manager): یکی از ابزارهای npm یا Yarn مورد نیاز است. (npm به طور پیش‌فرض با Node.js نصب می‌شود.)۲. فرآیند نصب و راه‌اندازی (Installation and Setup)برای دریافت و آماده‌سازی پروژه، دستورات زیر را به ترتیب در محیط ترمینال (Terminal) یا خط فرمان (Command Prompt) اجرا کنید:۲.۱. کلون کردن مخزن (Cloning the Repository)مخزن پروژه را با استفاده از دستور زیر از گیت‌هاب کلون نمایید:git clone [https://github.com/KhodePouria/FacePong.git](https://github.com/KhodePouria/FacePong.git)
۲.۲. ورود به دایرکتوری پروژهپس از اتمام عملیات کلون، به دایرکتوری اصلی پروژه وارد شوید:cd FacePong
۲.۳. نصب وابستگی‌ها (Installing Dependencies)تمام پکیج‌ها و کتابخانه‌های مورد نیاز پروژه را با استفاده از یکی از دستورات زیر نصب نمایید:با استفاده از npm:npm install
با استفاده از Yarn:yarn install
۲.۴. اجرای پروژه (Running the Project)برای راه‌اندازی برنامه در حالت توسعه (Development Mode)، از دستورات زیر استفاده کنید:با استفاده از npm:npm start
با استفاده از Yarn:yarn start
پس از اجرای موفقیت‌آمیز دستور، برنامه به صورت خودکار در مرورگر پیش‌فرض شما در آدرس http://localhost:3000 باز خواهد شد.۳. دستورات کاربردی دیگر (Other Utility Scripts)دستورشرح عملکردnpm run build / yarn buildایجاد نسخه نهایی و بهینه‌سازی شده (Production Build). فایل‌های خروجی در دایرکتوری build قرار می‌گیرند.npm test / yarn testاجرای تست‌های واحد (Unit Tests) پروژه (در صورت وجود).۴. تکنولوژی‌های اصلی (Core Technologies)این پروژه بر پایه React و ابزارهای مرتبط با آن توسعه یافته است. از آنجایی که نام پروژه "FacePong" است، انتظار می‌رود که از کتابخانه‌هایی برای تشخیص و پردازش چهره (مانند face-api.js یا مشابه آن) نیز استفاده شده باشد..
